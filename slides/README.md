# Partner engineering briefing

Run the four-slide deck locally:

```bash
cd slides
bun install
bun run dev
```

Open `http://localhost:3030`.

Build the static deck or export the PDF:

```bash
bun run build
bun run export
```

The exported briefing is written to `docs/partner-engineering-briefing.pdf`.
